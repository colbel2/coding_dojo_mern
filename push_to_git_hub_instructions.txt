git add .

git commit -m "note about what you are uploading"

git push




git clone then copy and paste the link to the entire main page of the repository you are tryng to copy 


git add .

git commit -m "note about what you are uploading"

git push




git clone // then the github address of the repository you want to download to desktop


git pull //then the github address of the repository that already has been cloned - note you have to be inside the parent folder of the repo file